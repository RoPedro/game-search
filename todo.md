## Add lowest prices for games
### Workflow:

- get Steam ID using IGDB (If exclusive, fallback(Maybe Slug))
- assign to platform_id (name in progress)
- search price on ITAD on all major platforms
  - For PC, we will search for the cheapest Steam key (No Ubisoft/Microsoft Store allowed)
